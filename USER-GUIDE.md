# TYPO3 user guide

See [README](README.md) for account connection, consent settings, secure storage, billing, recovery and scheduled refresh. Site Configuration remains the source of public tracking preferences; the backend module manages encrypted connections independently.

Manual installations continue to work. Starting account onboarding explicitly switches the site to managed mode; no tracker loads until connection and current access are confirmed. Disconnect before switching back to the manual configuration.
